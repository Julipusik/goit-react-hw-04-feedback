import { styled } from "styled-components";

export const Stats = styled.div`
    width: 300px;
    display: flex;
    justify-content: center;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 24px;
    margin: 0 auto;

    p {
        margin: 0;
        font-size: 18px;
    }
`